# gcd -- Euclid's algorithm: recursion with modulo (%).

fn gcd(a: Int, b: Int) -> Int {
  if b == 0 {
    a
  } else {
    gcd(b, a % b)
  }
}

fn main() -> Int ! {IO} {
  print(gcd(48, 36));      # 12
  print(gcd(1071, 462));   # 21
  gcd(270, 192)            # returns 6
}
